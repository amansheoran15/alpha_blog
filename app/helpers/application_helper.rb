module ApplicationHelper
  # Shifted to application_controller
  # def current_user
  #   @current_user ||= User.find(session[:user_id]) if session[:user_id]
  # end

  # Shifted to application_controller
  # def logged_in?
  #   !!current_user
  # end
end
